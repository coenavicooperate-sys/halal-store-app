# ============================================
# Streamlit Secrets Configuration
# ============================================
# Copy this file to "secrets.toml" and fill in your values.
# NEVER commit secrets.toml to Git!
#
# For Streamlit Community Cloud:
#   App Settings > Secrets に同じ内容を貼り付けてください。

# Google Apps Script Web App URL (deployed URL)
WEBHOOK_URL = "https://script.google.com/macros/s/YOUR_SCRIPT_ID/exec"

# Access code for form protection (shared with store owners)
ACCESS_CODE = "your-secret-code-here"
